# SmartThings. Create a personal access token at
# https://account.smartthings.com/tokens (needs device and rules scopes).
SMARTTHINGS_PAT=your_token_here

# Local postgres. docker-compose reads these.
POSTGRES_USER=gridline
POSTGRES_PASSWORD=gridline
POSTGRES_DB=gridline
